%h3= session[:logged_in]
%h3 Welcome to the Incompetence Tracker
- if flash[:notice]
  .flash= flash[:notice]

- if session[:logged_in] == nil or session[:logged_in] == false 
  = link_to 'login', login_path
  = link_to 'Signup', signup_path
- else
  = link_to 'logout', logout_path
